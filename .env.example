# Copy to .env and set your Gemini API key for live draft generation.
# Without a key, the server falls back to simulated streaming from static content.

GEMINI_API_KEY=your_gemini_api_key_here
PORT=3000
